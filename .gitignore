# Local notes, not for the public repo
CLAUDE.md
docs/

# Local dev artefacts
apps/upload/temporary/

# arXiv corpus build outputs (PDFs + rendered page JPEGs).
# Reproducible from scripts/arxiv-corpus/papers.json.
scripts/arxiv-corpus/data/

# Photo corpus build outputs (downloaded Unsplash/Pexels JPEGs).
# Reproducible from scripts/photo-corpus/photos.json.
scripts/photo-corpus/data/

# COCO calibration benchmark cache (annotations + downloaded subset).
scripts/coco-cache/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Environment
.env
.env.local

# Terraform working dir (provider binaries, local state pointer).
# Note: .terraform.lock.hcl IS committed (provider version pin).
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
